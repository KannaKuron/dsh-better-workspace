# AGENTS.md

面向后续在本仓库继续开发的 Agent / 贡献者。读完再动手。

## 项目一句话

「dsh-better-workspace」:DSH 插件,把侧边栏「工作区」列表升级为**层级树**——磁盘层按官方 owningParentFolder 语义嵌套(子目录工作区挂父目录工作区行下),这是主结构;**工作区分组自 v0.21.0 起是三态(issue #7):磁盘目录(默认)/ 磁盘 + 名称 / 仅名称**——前两态对应 v0.15-v0.20 的名称层开关(判定收敛在 `workspaceSlashOf` 与 `workspaceGroupModeOf` 迁移:旧快照显式值照旧、0.13 以前的快照保持分组);视图菜单「工作区分组」区与设置卡分段控件**双入口写同一个 store 键**;「仅名称」完全不理会磁盘嵌套(buildTree 用空父 Map,工作区全在根层按 / 分组);**会话分组仍按名称里的 / 解析、默认开启,不受工作区开关影响**;添加工作区:**两个 directoryFlow 洞都不占用**(v0.16.0)——侧栏入口只走官方交互(原生宿主 = 官方 OS 选择器;LAN/无显示宿主给一句提示,指向对话空态),对话空态入口自 v0.22.0 起由本插件渲染**侧栏同源的树**,其「添加工作区」直接渲染官方 occupant 组件(见不变量 2);重命名即时重排。**空分组自 v0.17.0 起可显式声明**(视图 store 的 `folders` 箱;区头图标按钮 → 新建分组对话框,树状选父级 + 会话/工作区两态单选;空分组右键可删)。

## 环境与工具

- GitHub 操作一律用 gh(已认证 KannaKuron);git 走本地代理(端点见工作区总纲 AGENTS.md),偶发 TLS EOF 原样重试。
- **本机启动 dsh 必须用 Homebrew node,不要用自带 runtime node(v0.23.0 实测)**:`/Users/kanna/.dsh/dsh-runtimes/dsh-primary-runtime/dependencies/node/bin/node` 是 Developer ID + hardened runtime 签名,而 `node-addon-require-builtin` 的 prebuilt 是 adhoc 签名 ⇒ `dlopen` 报 *mapping process and mapped file (non-platform) have different Team IDs*,`dsh web` 直接 fatal `No usable native binding found`。用 `PATH=/opt/homebrew/bin:/opt/homebrew/opt/node@24/bin:/usr/bin:/bin`(ad-hoc 签名的 node)即可。另一条等价路径是打包版 Electron 当宿主(`ELECTRON_RUN_AS_NODE=1 "/Applications/DeepSeek Harness.app/Contents/MacOS/DeepSeek Harness" /opt/homebrew/bin/dsh ...`,桌面端就是这么起的,带 `--expose-internals`)。
- 发布双通道同家族惯例:npm test → npm version → git push --tags → gh release create → Release published 触发 OIDC 自动发 npm(node 24 + id-token: write,见 .github/workflows/npm-publish.yml)。发布后 curl -X PUT https://registry.npmmirror.com/dsh-better-workspace/sync 同步 npmmirror。
- **校验发布是否成功要看 registry.npmjs.org,不能看本机 `npm view`(v0.23.0 实测)**:本机 `npm config get registry` 指向 `https://registry.npmmirror.com`,镜像有缓存延迟 —— 发布成功后头几分钟 `npm view dsh-better-workspace version` 仍显示旧版本,容易误判成「Action 绿了但没发出去」。正确做法:`curl -s https://registry.npmjs.org/dsh-better-workspace | python3 -c "import json,sys;print(json.load(sys.stdin)['dist-tags'])"` 看 latest,再 PUT `/sync` 触发镜像并轮询镜像的 dist-tags。
- 本机 web profile 装本地开发版:npm pack 出 tarball → 在 (dsh home)/profiles/web 里 pnpm add <tarball> → profile cordis.patch.yml 加挂载行(- insert: - id: better-workspace / name: dsh-better-workspace)→ 重启 DSH。

## 变更记录纪律(2026-09-13 起)

- **所有版本发布、修复、事故复盘、复现/验证记录一律写进本仓库 `CHANGELOG.md`**,不再追加进本文件;
  本文件只保留仍然有效的规则、不变量与当前事实,历史叙事由 CHANGELOG 承载(需引用时写
  「见 CHANGELOG vX.Y.Z」)。
- **发版流程新增强制步骤**:更新 CHANGELOG(写好新版本条目)→ 随版本提交 → 再打 tag /
  发 Release;顺序不能反。
- CHANGELOG 条目格式:倒序排列;`## vX.Y.Z — YYYY-MM-DD` + 类型(feat / fix / docs / chore)+
  要点 bullet + 相关链接(issue / PR / discussion / Release)。

## 目录地图

| 路径 | 作用 |
|---|---|
| src/client.js | **全部功能所在**。window.__ModuleLoader__.load({ id, factory }) 包装的浏览器插件:sidebar.workspaces 座位(priority -1)+ 官方目录流驱动(startAddFlow)→ 双设置座位 + 两层层级树 UI |
| src/index.js | host 半,纯占位(有效 Node 入口 + 一行日志);未来宿主侧功能(分组宿主登记、设置页后端)的家 |
| cordis.patch.yml | dsh plugin add 官方安装通道的挂载声明(insert 一行插件 row) |
| dsh.plugin.json | 插件注册表清单(id dsh-external/dsh-better-workspace) |
| tests/smoke.mjs | 冒烟测试(纯文件级,无 Cordis 运行时):清单一致性、基线 require 白名单、slot 注册、21 门词典逐门键集对齐、无 import/JSX/TS 语法 |

## 核心不变量(改代码前必读)

1. **无构建,单文件客户端**。src/client.js 必须保持 __ModuleLoader__ 包装;每个 npm 包只有一个 client entry(exports["./client"]),**不能 require 任何非基线模块**。基线白名单(dsh-client-web seed.ts):react、react/jsx-runtime、react-dom、react-dom/client、@deepseek-ai/cordis、@deepseek-ai/dsh-client-store、@deepseek-ai/dsh-client-ui-slots、@deepseek-ai/dsh-client-ui-primitives。冒烟测试强制执行。**exports 必须含 "./package.json"(issue #9,2026-09-23)**:桌面渲染进程的模块发现(ctx.loader.internal 不可用)回退 createRequire().resolve(<pkg>/package.json),该调用遵守 exports map——缺这一行抛 ERR_PACKAGE_PATH_NOT_EXPORTED 被吞,包被永久缓存为「非 client 包」,client 半永不进启动图且宿主日志全绿(极难排查;web 宿主走 internal 分支所以测不出来)。冒烟测试有断言。
2. **slot 契约**(对照 dsh 源码 packages/client/ui-workspace/src/client/):
   - sidebar.workspaces 是 **single** 槽;same-priority 二次注册会抛错,官方占用者优先级 0,本插件用 priority: -1(ascending, lowest renders)压制。
   - **两个 directoryFlow 洞都不占用(v0.16.0 定型;v0.12.1 的自占模式已废弃)**。v0.12.0 事故的根因是**子洞声明互斥**:官方 WorkspaceBrowser 的 entry 输掉渲染竞争但仍在 ledger 上、仍拥有 'sidebar.workspaces.directoryFlow' 的声明,bw 再声明同洞 → register 抛 `slot ... is already declared` → guarded 吞掉 → **bw 浏览器整个不注册,官方浏览器接管**(用户实测:右键菜单没了、/ 不解析)。
     **为什么"改用官方对话框"只能靠不占用**:`renderSlot` 授权绑定在**声明该子洞的 entry** 上(`ui-slots` 的 `children` 表 = 声明 + 授权 + 运行时规格三合一),而子洞声明独占;官方 entry 持有声明却不渲染(座位被 shadow),于是**接管座位的第三方永远拿不到渲染授权**(ui-renderer 硬检查 `not declared by this entry's children` → SlotOwnershipError)。**"自己的侧栏"与"官方对话框"在 dsh 当前版本下互斥**,而官方本就有一套更好的:宿主 `directory-picker-auto` 把 host 半与 client 半**成对挂载**,browse 后端下 `dsh-client-ui-directory-picker-browse`(`DirectoryBrowser.tsx` 1052 行,分栏式)照样加载并注册进两个洞。**绝不要再尝试 children 声明,也绝不要恢复自绘浏览器**(冒烟测试两条防回归断言)。
     现行形态:侧栏添加按钮由 `flowAvailable`(hooks.directoryFlow → useDirectoryFlow,占用者是官方 occupant)门控 → `startAddFlow()` 先 `pickerCapabilityNow()`:`native` 直接调官方 `uiWorkspace.pickDirectory()` 弹 OS 选择器,`browse` 弹 `add-guide` 极简提示指向对话空态;采纳语义不变 = `adoptDirectory`(`createWorkspace` + `startSession`)。
   - **对话空态的工作区座位自 v0.22.0 起也由本插件接管**:`conversation.hero.workspace`(single/root,官方 picker 优先级 0)同样 `priority: -1`,弹层换成**侧栏同源的树**(buildTree + compressTree + 同一个 viewStore 的 groupBy / workspaceGroupMode / compactChains),所以侧栏任何视图选项改动都即时同步;侧栏切到「单列表 / 按工作区」时退化为一层完整标题(选择器必须能选到每个工作区)。**该座位同样不声明任何 children**:官方 entry 的 `conversation.hero.workspace.directoryFlow` 声明照旧,占用者(native/browse)照常注册,本插件用 `slots.entries(HERO_FLOW_HOLE)[0]` 读出**占用者的组件 + 它自己的 inject 面**,补上 owner 对话(open/busy/onPicked/onCancel/onError)后直接渲染——这是"不声明子洞又想用官方 occupant"的唯一可行路径;**ledger 读不到只是少一个「添加工作区」项**(`heroFlowEntry()` 已 try/catch),选择器不退化。菜单行自绘(`button[role=menuitem]`,官方 Menu 的键盘走查只认这个选择器 + `button:not(:disabled)`)——官方 `MenuItemButton` 的 `.itemIcon` 是**固定 14px**,塞进缩进会被裁掉,所以行内布局(缩进槽 / chevron 或文件夹字形 / 标题 / 勾选)全部自己画,列表卡片、滚动、外部点击、方向键走查、焦点归还仍由官方 `Menu` 提供(`portal` + `getAnchorRect` 指向 owner 的 chip)。宿主没有 primitives `Menu` 时**不注册**该座位(官方 picker 继续工作)。
   - **官方座位占用者的通用读法(v0.23.0 起,两处使用)**:凡「官方 entry 声明、官方 entry 自己消费」的座位/通道,被 shadow 后都不能靠注册拿走,只能按上面那条 hero flow 手法读 ledger:`slots.entries(<seat>)[0]` → `{ component, inject(), locale }`。**两个实例**:① `sidebar.session.row.leading` 的定时任务角标(`scheduleSeatOf` + `ShippedSeatOccupant`),`hooks` 舱按 ui-slots 的 `use<Name>` 契约绑定(`bindOccupantFace` / `bindSelectorHook`,语义 = 官方 `bindSnapshotSelector`,但那个 `use-sync-external-store` shim 不在基线白名单;`React.useSyncExternalStore` 不存在时 `scheduleSeatReader()` 直接返回 null),官方行规则照搬(仅行主状态空闲、blank/归档留空),外层挂 `QuietBoundary`;② `sidebar.workspaces` entry 的注入面(`shippedBrowserFaceOf`),拿 rc.2 的快捷键请求通道与键位目录。**读法纪律**:每次都带形状门控(缺 `requestSearch` 就是老宿主 ⇒ 无通道)、全部 try/catch、缺席时降级为「什么都不做」而不是自绘替代品。
   - **≤rc.1 与 rc.2 的互斥双源(v0.23.0)**:同一个功能在两代宿主上分别只有一条数据源,代码里必须**互斥门控**,绝不允许双份渲染 —— 例:定时任务角标在 ≤rc.1 读 `projectionValues.schedule` 自绘(rc.2 已删该投影),rc.2 读上面的座位占用者;门控是 `node.hasActiveSchedule && !markNode`,冒烟有断言。
   - **目录后端探测仍是红线,但只用于"决定按钮行为"**:宿主只组装一个后端(`@deepseek-ai/dsh-host-directory-picker-auto`)——`bindHost === '127.0.0.1'` 且非 SSH、有可用显示会话才是 `native`,**其余一切绑定(含 `0.0.0.0`/局域网)都是 `browse`**;browse 的 wire 动词只有 `list` / `createDirectory`,`pick` 会被拒为 `directory-picker/unavailable`。`startAddFlow()` 先问 `pickerCapabilityNow()`(`list` 成功即 browse、以能力码被拒即 native、无法归类则**不缓存**并照旧试 `pick`):native → 官方 OS 选择器;browse → `add-guide` 提示。**绝不再假定 native**——≤0.11.2 正是这一假定让 LAN 绑定下「添加工作区」必然失败。
     **v0.16.0 删掉的东西不要恢复**(冒烟测试有断言):`DirectoryBrowseDialog` 与它的面包屑/盘符探测(模块级 `browseDrives`)/`createFolderIn` seam、`.bw-browse-*` 33 条样式、21 门语言的 13 个 `browse.*` 键、`BetterFlow` 占用者、`markPickerBrowse()`。用户实测结论是"官方的更好用",而官方那套确实更好(Finder 式分栏);仿制没有意义。
   - BetterBrowser 的 flowAvailable(hooks.directoryFlow → useDirectoryFlow)是添加按钮的门控:`true` 表示宿主组装了某个后端包(官方 occupant 在洞里),这才是"能添加"的诚实信号;没组装就藏按钮。flow hooks 必须在 `if (!wide) return` early return 之前(React hook 纪律)。本插件不再有 occupant 组件,添加动作由 `startAddFlow()` 直接驱动(见上一条),采纳语义收敛在 `adoptDirectory`(createWorkspace + startSession)。
3. **数据事实**(dsh 0.1.2-alpha.1 实测):
   - 分组来源是 WorkspaceView.title(**引号感知 + URL 兜底切分** splitTitleSegs:① “…”/"…" 成对引号段**原样**,段内 / 绝不切(孤立引号——无匹配闭端的开引号或无开端的闭引号——只是普通字符,照常参与切分;0.9.1 曾让未闭合开引号吞到串尾,用户明确不要);② 引号外文字走 splitPlainSegs——从第一个 :// 起尾部整体单 leaf(0.8 曾按 URL 语义聚树,用户实测后明确不要任何 URL 层级,0.9.1 回退),:// 前照常按 / 切)。**会话标题的 quote-on-land effect**(见不变量 4)是主机制,无引号兜底是显示层保险。title 为空回退 basename(path)。**buildTree(v0.12.0 重写)是两层正交**:磁盘层用 diskParentMapOf(normDiskPath:Windows 分隔符归一 + 去尾分隔符,最长真前缀胜出)求「最近已注册祖先工作区」,子工作区构成父 wsEntry 的 .sub 层;名称层 ensure 在每层内做(段数组驱动,join 后的 path 只作 Map 键,绝不再 split 回拆);sub 层的 folder 带 idPath(拥有链前缀 + '//' + 名称路径)作折叠/样式身份键,path 保持语义键(rename 批量改写用)。countWorkspaces/pulse/findTreeNode/renderLevelRows 都递归 sub。**buildSessionTree 的 ensure 同样段数组驱动**。
   - 会话标题是 SessionSummary.displayTitle(blank 会话显示本地化「新会话」);SessionListState = { ids, byId, current, phase, ... };WorkspaceSnapshot.archivedSessionIds 是注册表级归档集。**冷列表 title 缺失事实(0.1.2-alpha.4..0.1.5-alpha.1 溯源,projection-cache/title 包两版本 0 diff)**:列表行的 title 只在命中持久投影缓存时携带;fork(seeded)会话在 dsh list.ts 里被整体跳过必缺,从未写过检查点的低活动会话同样缺;缺失时 displayTitle 已是宿主 basename 回退(displayTitleOf:title → cwd basename → id),真实标题仍在会话日志的 session/title 事件里、只是没有便宜的列表读取路径。0.9.4 的 remembered 兜底只补显示层,不改这个事实。
   - **会话可见性官方规则**(tree.ts sessionVisible):origin 非 subagent、不在归档集、blank 仅当前选中可见。**归档筛选三态(v0.23.0,rc.2)**:`archivedFilter` = `default`(隐藏已归档)/ `show`(全部对话)/ `only`(仅显示已归档);**`only` 档下没有归档会话的工作区整行不渲染**(官方 tree.ts `if (archivedFilter === 'only' && members.length === 0) continue`,本插件用 `workspaceVisible()` 复刻,分组递归判空),空列表显示 `empty.noneArchived` + `empty.viewOthers` 回退按钮。子代理行(Side:*)永远不进浏览器列表。SessionSummary 还有 completed(完成提醒)、parentId(子代理血缘;客户端 service 映射 parentSessionId → parentId,**读 parentId**,旧拼写仅作向下兼容兜底——2026-08-31 修正)。
   - **活跃定时任务角标(两代,互斥)**:≤rc.1 官方行读 `projectionValues.schedule`(v0.6.0 起本插件同样接入,`IconAlarmClockOutline16` + role=img/aria-label/title);**rc.2 删掉了该投影**,角标改由座位 `sidebar.session.row.leading` 的官方占用者渲染 —— 本插件按不变量 2 的 ledger 读法在自绘行里渲染该占用者,旧自绘角标以 `!markNode` 门控保留给老宿主。详见不变量 2 与 CHANGELOG v0.23.0。
   - **状态灯官方语义**(Rows.tsx sessionStatuses + StateDot):pending(approval/plan-review/question → warning 琥珀)> running(ongoing 蓝色运行环)> 子代理运行中(ongoing)> completed===true(done 绿);空闲不显示灯。绝不要用自绘圆点代替 StateDot。
   - 浏览器组件 props:wide/expandSidebar(owner)、useWorkspaces/useSessions/useSessionPendingInteraction(standard)、注入动作(startSession/open/renameSession/forkSession/renameWorkspace/deleteWorkspace/archiveSession/createWorkspace/searchSessions/insertWorkspaceBefore/insertSessionBefore——最后两个由本插件 browserInjected 特征探测提供,分别桥接宿主 workspaces.insertBefore 与 workspaces.insertSessionBefore,见官方 slots.ts 契约;v0.12.0 起不再注入 pickDirectory/listDirectory/createDirectory,拾取交互归官方占用者)、useStore/actions(store)、t(locale)。注入工厂的非 hooks 条目按原名成为 props;hooks 舱条目被渲染器绑成 useXxx 钩子。
4. **收起语义**:工作区行点击 = 该工作区会话树整体 0↔全部(默认展开,收起后行上保留会话数角标);会话子分组默认展开、可收起;搜索时强制全展开。**不要**恢复旧的「收起仍显示 5 条」行为(用户明确要文件夹式收起)。**展开后的限量显示是另一回事(v0.21.0,issue #8)**:prefs.sessionLimit(0=不限量默认;5/10/25 档)开启时,展开的工作区先只显示前 N 条(blank/running/子代理/置顶不占额度,官方 collapsedSessionRows 语义),尾部「展开 {n} 个会话」行(复用遗留的 sessions.expand 键)点击后本次挂载内全量(组件态 limitLifted,不持久);搜索永不限量;官方 flat 视图不限量。行上 + 新建会话会**强制展开**该工作区(onStart 里 setSessionsExpanded(id,true) 先于 startSession——用户必须看到新会话出现)。**quote-on-land(0.9.2 修正)**:只对「本挂载内亲眼见过 blank 快照的会话」生效——blank 是新会话的唯一可信出生标记(0.9.1 按「首轮快照后首次出现」判新,store 渐进填充时存量旧会话被误包引号,实测事故);落地后不立即改名,过 **20s 稳定期**(TITLE_STABLE_MS),期间标题再变即重置——线上分不出确定性兜底标题(回显首条消息,易带 /)与异步 LLM 正式名,等稳定才整串包 “”;LLM 名(通常无 /)到达即放过。**用户改名(重命名弹窗/拖拽入组/分组前缀改写)必须经 renameByUser 标记 humanTouched,永不自动包引号**(宿主端用户名 pin 住自动命名,后续不会被覆盖;新增用户改名入口时必须接同一包装);fork 子会话出生即 titled 无 blank 不触发;失败不重试(显示层兜底仍平铺)。客户端拿不到标题来源字段(SessionSummary 只有 title/displayTitle),blank+稳定期是唯一近似。**状态呼吸灯**(prefs.statusPulse 默认开;0.9 起取代 0.8 的完成 toast,toast 全套已删):被折叠遮蔽的状态沿层级向外冒泡到最近可见容器行——工作区/分组行让 **icon 呼吸发光**(drop-shadow,keyframes bw-breathe;icon 为 none/缺失时回退官方 StateDot;行自定义过标题发光(textShadow)的 label 同步呼吸 bw-pulse-text),会话分组行(无 icon)在行首放呼吸 StateDot。**冒泡只含 warning 琥珀(#d29922)> done 绿(#3fb950),ongoing 一律不外渗**(relayStateOf 过滤)——运行中的蓝色呼吸只出现在会话行本层(官方运行环包 PulseGlow),用户明确觉得 running 外渗吵。聚合:nodePulseOf(会话子树)/wsPulseOf(仅折叠工作区)/folderPulseOf(折叠文件夹聚全部,**无视内部展开状态**——折叠文件夹下展开的工作区同样不可见)。**教训:useEffect 的 deps 数组在渲染期求值**——引用后面才声明的 const(如 searching)是 TDZ ReferenceError,会让整区 QuietBoundary 降级空白(2026-08 实测);deps 需要的派生值(normalizedQuery)必须先声明或用内联布尔表达式。
5. **会话层级**:会话标题同样按 / 分层(buildSessionTree);会话子分组**必须**与工作区文件夹视觉可辨——次级配色(tertiary)、无文件夹图标、只有 chevron。分组重命名 = 批量改写成员标题前缀(renameSession)。
6. **持久化**两个 key:**dsh.betterWorkspace.view.v1** 走 dsh 客户端 store(defineStore + persist;折叠状态 expanded/sessionsExpanded/sessionGroups、prefs、styling、**sessionOrder(本地会话序,0.10.2)**、**folders(显式空分组,v0.17.0)**;`folders.ws` 按磁盘层 idPrefix 分箱、`folders.sess` 按 workspaceId 分箱,值都是路径列表;声明**与成员共用同一套树节点**(`ensure` 幂等),所以"先建空组再拖进去"与"按名字建组"等价;老快照无该键,所有读取走 `EMPTY_FOLDERS` 兜底);**sessionOrder 与折叠状态同类:每端本地,绝不进跨端同步**——它只是宿主 reorder 通道消失时的兜底。**dsh.betterWorkspace.titles.v1** 自 **0.9.6** 起是**普通 localStorage 缓存**(模块级 titleCache 对象 + loadTitleCache 整值恢复 + scheduleTitleSave 200ms 防抖整写,{ byId: { id: { title, at } } })——issue #1 证明它绝不能做成响应式 store。不要为动态实验另起第三种持久化。
   - **关键事实**:hydration 是整值替换(attachPersistence 直接 setState(JSON.parse(raw))),不会合并 init。新增 state 键必须同时:(a) 初始化默认值;(b) action 里容错缺失键;(c) selector 读取带回退。2026-08 曾因 sessionGroups 未容错导致「会话分组点不开合」——这是本仓库的硬性纪律。
   - **设置面双时代(v0.19.0 起,dsh 0.1.7 分界)**:host 半静态导出 `Config`(四字段全 volatile 探测,行 id `better-workspace` 与旧命名空间同串),旧宿主的 settings.register 路径原样保留;client 半的 inject 数组不再硬声明 `settingsScope`(0.1.7 删除了该服务,硬注入会讧 fiber 永远 PENDING),改为旧式软探测 ctx.settingsScope(已有)+ 新式 ctx.inject([configForms], ...) 取 configForms.get(better-workspace)(与旧 scope 同契约:getSnapshot/set/subscribe,写入落到本 profile 的 patch)。跨端同步区只在 settingsScope 时代渲染:0.1.7 起设置按 profile 存储,共享的 settings.yaml 之家不复存在,手动同步按钮隐藏(偏好仍双写到本 profile)。
- **跨端同步(0.9.5)是手动的,绝不自动镜像/自动迁移**(用户明确决定):宿主 settings 命名空间 better-workspace(~/.dsh/settings.yaml,web 与桌面共享 DSH_HOME)承载 styling(dict)/compactChains/statusPulse/appearance(folders 字段**不进跨端同步**(与折叠状态同类:每端本地)),作为第三层持久化;折叠状态永远每端本地。写路径 makeSharedWrites 双写(本地 action 即时回显 + scopeSet 写宿主);读路径不变(全走本地 store,拉取时 importHost/mergeHost 把宿主值灌进本地)。设置卡片同步区按 IS_DESKTOP_SURFACE(location.protocol==='dsh-app:')显示「从客户端获取」/「从 Web 获取」+ 覆盖(importHost 整值替换)/合并(mergeHost 并集、拉取方键冲突获胜)模式 + 「发送本设备数据」按钮(旧版本历史数据首次同步必须显式推一次)。scope 在 apply() 里 ctx.settingsScope.bind({namespace:'better-workspace'}) 一次;bind 失败/服务缺失 → 全本地降级(= 0.9.4 行为)。宿主落地条件:settingsScope persistence 由宿主 isLoopback 决定(web 127.0.0.1 与桌面 dsh-app transport ownsHost:true 都写宿主;远程非 loopback 页面为 memory 不落盘)。schema 仍必须 schemastery(host 半动态 import),styling 用 Schema.dict(Schema.any())——形状由插件自己管。
   - **title 缓存机制与红线(0.9.4 引入,0.9.6 重写)**:学习 effect 只记 summary.title(wire 真值,非空字符串;blank 行不学),**绝不学 displayTitle**(它正是回退产物);渲染经 sessionTitleOf 第三参兜底,**优先级:wire title > remembered > displayTitle 回退**;超 TITLE_CACHE_LIMIT(3000)按 at 淘汰到 2400,淘汰每批只跑一次;stale 记忆(他端改名)在 wire 到达后自愈。**0.9.6 形态(issue #1)**:普通对象 + rememberAllTitles(list) 每快照**一次批处理**(同值零分配、只有真变化才置脏),变更经 scheduleTitleSave 200ms 防抖**整写一次** localStorage;loadTitleCache 在 apply() 首渲染前同步恢复;titleCacheRef = { getSnapshot, rememberAllTitles } 组件直读不订阅。**红线:绝不回到响应式 store/逐会话 dispatch**——0.9.4-0.9.5 的 per-session rememberTitle 每次 action 都是 immer produce + sync 广播,O(会话数)次/快照 tick,大会话装(数百会话 × agent 运行时 ~44 tick/s)直接冻结主线程(实测 41% CPU、点击冻 0.9s);「reducer 内 no-op」不等于「action 免费」。
7. **拖拽语义**(原生 HTML5 DnD,drag 状态机 { kind, source, over }):
   - 工作区(v0.12.0):drag.source 携带 disk(diskParentMap 亲算)。同名称分组拖到工作区行上/下半 → insertWorkspaceBefore(anchor);跨名称分组 → renameWorkspace(新前缀)+ insertBefore;拖到分组行(idPath 身份,提交时剥掉 '//' 前缀还原名称 path)→ 移入该分组(rename + append)。**磁盘层是文件系统事实,不随拖拽改变**:跨磁盘层的 drop 只重组名称前缀(anchor=undefined → insertBefore(id) 追加,落位自身磁盘层末尾),同磁盘层才走锚点序。root 分组 = 无前缀标题。搜索中禁用拖拽;**「仅名称」模式(v0.21.0)下没有磁盘层,一切 drop 退化为名称前缀编辑,跨磁盘层特殊规则天然不触发**;
   - **v0.15.0:名称层关闭时,工作区 drop 必须是纯排序,标题绝不被改写**。拖拽源的 leaf/folderPath **一律**从原始标题推导(见下一条的 0.12.0 教训),而关闭时树里每个 entry 的 folderPath 恒为 ''——两边不一致会让**每一次** drop 都被判成"跨组移动"并静默 renameWorkspace(前缀被吃掉,树上一个组都看不见却改了名字)。修法两处对齐:拖拽源在关闭时不携带名称前缀,**且**提交处 `!workspaceSlash ||` 再兜一道(防拖拽途中切换开关);**新增任何读 source.folderPath 的路径都要过这个开关**。
   - **工作区拖拽激活期间 compressTree 暂停**(drag.kind==='workspace' 时 tree memo 跳过压缩,依赖是 draggingWorkspace 布尔而非整个 drag 对象——over 高频变化不该重建树):合并行把链上所有分组层级藏进名字里,恰好删掉了「移入该分组」的投放目标;拖拽期间单链展开回文件夹行、任意一级可投放,拖完自动合并回去。**拖拽源身份(leaf/folderPath)必须从原始 title 按 / 切分推导**,绝不能读合并行的显示 leaf(其 folderPath 被重置为 ''),否则拖着合并行做移入/跨组放置会拼出 x/组/工作区 这类双重前缀标题;
   - **工作区 dragstart 的 setDrag 必须延迟一帧**(wsDragArmTimer setTimeout 0,dragEnd 清理):Chromium 在拖拽手势建立前,若同步重渲染把拖拽源元素从光标下移开(链展开恰好会把新行插到拖拽行上方),会立即取消整个拖拽(0.7.0 回归,参照 react-dnd #3649 的结论与同款 setTimeout 解法)。会话拖拽不改树结构,保持同步 setDrag 不受影响;
   - 会话:同工作区内拖到会话行上/下半 → insertSessionBefore(以扁平 sessionIds 序为锚);拖到会话子分组行 → renameSession(新前缀标题)。跨工作区拖拽被守卫拒绝。**v0.18.0:可见顺序由分组决定,跨组 drop 必须真的改归属**——落在**别的组**的会话行上时先 `titleMovedToGroup(source.title, target.title)`(源叶子 + 目标行的前缀,目标在根层 = 去前缀)再按锚点排序;落在**自己的工作区行**上 = 移出分组(去前缀 + append 到根层末尾,拖拽经过时该行 `bw-drop-into` 高亮)。**组外任意处都是出口(v0.18.0)**:`.bw-root` 容器自己也接住会话拖拽(行级 stopPropagation 之后才轮到它),拖到树里任何不属于分组的地方松手 = 移出分组;这类拖拽期间容器挂 `bw-root-drop-out`(底部一条淡虚线),提示存在这个出口。归属已经对上的行不接管、不高亮(根层会话拖到工作区行、或拖到同组行,都只走纯排序);跨组提交里 `renameByUser` 永远先于 `reorder`,insertSessionBefore 缺失时仍回落本地序。右键菜单 `move-out` 提供同一个动作(只在会话标题带分组前缀时出现)。**会话行排序有两条通道(v0.10.2)**:宿主注入的 insertSessionBefore 是首选(权威、与其他界面一致),dsh 0.1.6-alpha.1 已把它从浏览器契约移除 → 此时落到**浏览器本地扁平序**(store.sessionOrder,按 workspaceId 存 id 数组;reorderIds 复刻 insertSessionBefore 的锚点语义:锚点缺失即追加)。**本地序只在宿主无该动作时生效**(sessionsOf 里 `typeof insertSessionBefore === 'function' ? [] : sessionOrderOf(...)`),宿主序永远优先。绝不能裸调 undefined(≤0.7.1 拖会话排序即弹 TypeError: insertSessionBefore is not a function,根因是 browserInjected 漏注入该动作);会话行 draggable 从不因此关闭:拖到子分组是纯 rename,永远可用。
   - 显示顺序 = 宿主手动序(sessionIds / registry 序),本地序仅在宿主无 reorder 通道时接管;buildTree/buildSessionTree 只排序分组名,绝不按名称/时间重排成员行(否则拖拽结果不可见)。
7. **词典纪律**:NS = betterWorkspace。`zh` / `en` 是文件内的两本词典,其余 19 门语言在 `LOCALES` 表里一门一条、每条前一行 `/* locale: <tag> */` 标记(ar / de / fr / hi / id / it / ja / ko / nl / pl / pt / ru / sv / th / tr / vi + zh-HK / zh-MO / zh-TW;繁体港式与台式各一份,zh-MO 与 zh-HK 共用同一份)。**每本词典的 key 集必须与 zh 完全相等**——缺键不报错、查表时静默回退英文,面板会变成半翻译状态,冒烟测试逐门比对键集(缺一个就红);同时覆盖文件里每个静态 t(...) 调用,动态拼 key(如 time. + unit、status. + kind)的取值集合也要配齐。**加文案键时:zh / en 一起加,再给 LOCALES 里每一门语言都加**;加一门语言 = 在 LOCALES 追加一个带标记的条目,不改任何逻辑。词典经 `ctx.locale.register(NS, Object.assign({ zh, en }, LOCALES))` 一次注册给 DSH 的 locale 服务。
   - **本插件不自己解析词典、也不自己订阅语言变化**:`t` 是宿主渲染器按 slot 注册里的 `locale: NS` 绑好的座位(ui-renderer 的 localeSeat → LocaleRuntime.bind:每次查表读当前 active,语言切换经 locale revision 让每个 outlet 重渲染,React.memo 也按新引用重算),所以这里**不要**引入 dsh-ide-git 那样的 translatorOf / dictionaryFor / LocaleLive——那是给「宿主服务不到的自绘面」(better-sidebar Tab)用的,本插件所有界面都走 slot 座位。语言能否被选中由 DSH 的语言目录(catalog)决定:本插件只登记词典,把某门语言加进可选目录是语言包插件(`ctx.locale.addLanguage`)的事——本插件绝不调 addLanguage,否则会与语言包抢注册直接抛 already registered。
8. **React 纪律**:纯 React.createElement;组件必须定义在模块层(内联组件定义会在父组件每次渲染时重挂载、丢输入状态);所有 hook 调用必须先于任何 early return。
9. **防御式边界**:primitives 图标/组件一律经 icon()/BTN() 特征探测降级,不硬崩;require 只允许基线(测试强制)。**host 半必须能在「peer 依赖解析不到」的 profile 里加载(0.21.1 加固)**:`src/index.js` 顶层**不得**静态 import 任何 peer(`@deepseek-ai/schemastery` 是 peer,普通 Node 从本包位置根本解析不到它,只靠宿主的 profile shared fallback 供上来;一旦那次解析失败,静态导入即失败,而 Loader 把导入失败当非致命跳过,fiber 永不建立,client 半因此不进启动图,官方 occupant 静默接管座位、宿主日志全绿,与 issue #9 同一失败类——已用"除这一行外完全相同"的夹具插件实证:静态版 `apply()` 从不执行、惰性版照常执行)。一律 `await import(...)` + try/catch,拿不到就降级(Config 导出 undefined,cordis 对 `!runtime.Config` 放行配置);冒烟测试有防回归断言。**同理,探测「可能不存在的服务」走 `ctx.get(name)`,不要读 `ctx.<service>`** —— 0.1.7 起在未注入时读属性会在 Context 代理上抛错,虽然 try/catch 能兜住行为,但每次启动都会在 console 写一条插件自报错误的堆栈。
10. **dsh peer 声明(0.21.1 起)**:`peerDependencies` 必须有 `"@deepseek-ai/dsh": ">=0.1.0"`(与 `engines.dsh` 同口径、**不设上界**),并把 `@deepseek-ai/dsh` 与 `@deepseek-ai/dsh-settings` **都**标进 `peerDependenciesMeta.optional`。rc.1 的兼容性门禁(`packages/boot/app-boot/src/plugin-compatibility.ts`)只读 `peerDependencies` 里 `@deepseek-ai/dsh*` 的 range,不声明就永不被校验;不设上界是因为本插件跨版本靠运行时探测自愈,上界只会在未来 dsh 升级时先行停用插件(`dsh-any-background@0.3.0` 的教训)。optional 是安装期硬要求:门禁不读 meta,但 `autoInstallPeers` 开启时包管理器会去 registry 解析 range,而这两个包在 npm 上全是 prerelease、普通 range 按 semver 排除 prerelease ⇒ 不标 optional 会让安装整体失败(`ERR_PNPM_NO_MATCHING_VERSION`)。冒烟断言锁死。

## 已知限制(改之前先看是不是已排期)

- 会话**跨工作区**拖拽仍被守卫拒绝(会话归属工作区);同一工作区内的分组归属自 v0.18.0 起随拖拽生效(别组的行 → 改写前缀,自己的工作区行 → 移出分组,右键另有「移出分组」)。
- 搜索是本地标题过滤,未接 session.search 宿主内容搜索。
- 外观自定义的「发光」是文字级 textShadow(浏览器合成),不改变主题 token;颜色/发光只影响本插件树行。

## v0.4 追加不变量(设置页 / 压缩树 / 右键外观)

10. **压缩树**(compressTree):仅当一层恰好一个子级时合并——单文件夹链合成一行(名字用 / 连接,path 取最深 = 展开状态键);单工作区链合成工作区行(leaf 拼接,kind=ws)。**0.8 起为相对段制**:递归只向链前插 node.name 段、链顶 materializeChain 才拼显示名,且相对第一个未压缩的祖先(VS Code 同款;链 a/b/c/W 在多孩子文件夹 a 下显示 b/c/W)——≤0.7 旧实现每层都前缀完整 node.path,嵌套链渲染出 1/2/1/2/3/A 类重复前缀。**只对根的子文件夹应用,根自身绝不合并**;纯展示层变换,工作区原 title/workspaceId 不动。ws-kind 在工作区/会话树遍历处都要处理(搜索、渲染、计数)。**工作区拖拽期间压缩暂停是特性不是 bug**(见不变量 7):合并行藏掉了链上所有分组的投放目标。
11. **右键即操作,可切换(v0.20.0)**:会话动作面是官方 0.1.7 Rows.tsx 的镜像——菜单条目(置顶/重命名/分叉/归档-取消归档)按官方顺序与图标对齐,文案经 officialT(官方 workspace 词典绑定,缺键回退 t(),键探测 = ot(key) !== key);设置卡两个开关:sessionMenu(默认开 = 右键开菜单;关 = 行悬停显示官方样式 ⋯ 按钮开同一菜单)与 rowActions(默认开 = 行尾置顶/归档快捷按钮)。会话行右键坐标仍来自事件;归档行走官方语义(灰显、不可开、点击解释、菜单变取消归档);置顶行走官方语义(分组内置顶前置、PinFill 角标、归档行无置顶);运行中会话归档被拒(WorkspaceArchiveError)时弹「停止并归档」确认(archiveSession 传 stopActivity: true)。工作区/分组行菜单沿用自绘;菜单动作(rename/delete/fork/archive/rename-folder/rename-sgroup/customize)。**v0.17.0 起分组行菜单 = 重命名分组(+ 空分组才有「删除分组」)+ 移动到分组… + 自定义外观**;「删除分组」**只对空分组提供**——非空分组是成员标题的投影,删声明在屏幕上什么都不会变,提供它就是撒谎(`ctx.payload.empty` 门控)。插件自己的条目一律排在分隔线**之后**,与官方条目分开(用户报的菜单卫生问题)。**v0.15.0 起四类行各有「移动到分组…」(`menu.moveToGroup`),但按开关逐层门控:工作区行 / 工作区分组行只在 workspaceSlash 开启时出现,会话行 / 会话分组行只看 sessionSlash**——容器不存在时不得承诺它;同一个 dialog 还服务于区头「新建分组」按钮(picker 形态,候选按开启的层收窄)与它的拖拽投放区(未开启那一层的行拖上去不高亮、不接管)。
12. **外观自定义**:store.styling 键约定 folder:/workspace:/session:/sgroup: <id|path>;值 { color, glow(整数 0..14,连续), icon, weight(400/500/600/700,可选), shadow(bool,可选) }。**icon 只对 folder/workspace 行有意义**(2026-08:会话行图标位被官方 StateDot 状态灯占用、会话子分组行无图标位,自定义 icon 从来不会显示;因此 CustomizeDialog 对 session/sgroup 不渲染图标网格、提交不带 icon——重存会顺带清掉旧遗留的 icon 字段)。icon 兼容矩阵:legacy 三值 solid/outline/none(实心/空心文件夹/无)或任意 primitives 图标名(`@deepseek-ai/dsh-client-ui-primitives` 是 `export * from './icons'`,全量导出)。**v0.10.2 起图标集不再钉死**:ICON_CHOICES 只是「候选清单」,出口由 `ICON_PICKER_CHOICES` 运行时过滤(只留本宿主导出的字形、按解析后名字去重),已保存的旧值经 `resolveIconName` → `ICON_ALIASES` 落到等价替代——宿主退役某字形时(dsh 0.1.6-alpha.1 删了 IconSendOutline16)网格自动少一格而不是多一个空格,旧的图标选择也不会变成空图标位。**改图标集必须同时动三处**:ICON_CHOICES(候选清单)/ ICON_ALIASES(退役名 → 替代链)/ ICON_PICKER_CHOICES(过滤逻辑);新增字形一律先提交进候选清单,旧宿主会自动滤掉。**发光只有文字**:textShadow 光晕只作用于行内文字,禁止行外圈 boxShadow(用户明确不要外框发光);**字体阴影是独立的第二个 textShadow**(`1px 1px 2px rgba(0,0,0,.85)`,与发光叠加)。外观对话框底部带**实时预览**(改颜色/发光/粗细/阴影/图标即时反映最终行效果)。**draft 的 seed 键是行身份不是 initial 对象(v0.22.0)**:owner 每次渲染都重建 `initial`,而运行中的会话让浏览器每个快照 tick 都重渲染——按 `initial` 引用 seed 会在编辑中途把 draft 重置回已保存值(用户实测「点开自定义后选颜色,几秒后闪回默认」,自定义手速被迫很快);CustomizeDialog 现在只在 `open` / `seedKey` 变化时 seed,`initial` 经 ref 读最新值,冒烟有防回归断言。default 提交 = null 清除;删除条目 = setStyling(key, null)。**字体描边(v0.10.0)**:styling 值新增 stroke(bool,可选)、strokeWidth(0.5..4,可选,step 0.5)与 strokeColor(**v0.10.1 起可手选**,默认**灰 #808080**;预设 灰/黑/白/品牌蓝 + 任意取色器,选 auto 才按该行最终字体颜色的 WCAG 反差取黑/白——contrastStrokeColor),行有自定义色时 JS 现算,无自定义色时读树容器采样的 CSS 变量 --bw-stroke-color(BetterBrowser 用 getComputedStyle 采样:html/body 属性 mutation 即时 + 1.5s 轮询兜底 → 主题与 dsh-any-background 之类插件的明暗切换都无需 React 重渲染);paint-order: stroke fill 保证描边在填充之下(字形不变细)。**默认外观(v0.10.0)**:prefs.appearance 是全局基底,行级 entry **逐字段**覆盖(老 entry 无 stroke 键照样继承默认描边);描边默认**开**、宽度 1px,其余字段默认同「无自定义」;宿主命名空间新增 appearance 字段参与跨端同步。**v0.10.1 修正**:`.bw-row-label` 的 overflow:hidden(省略号所需)会裁掉外描边——实测首字左侧缺边;用 `padding:1px;margin:-1px` 预留 1px 余量(文字起点/省略号行为不变,四向描边完整);行尾的计数与时间(`.bw-row-count`/`.bw-row-time`,11px 次级灰字)一律 `-webkit-text-stroke-width:0`,只让标题与分组名等主干文字带描边。
13. **设置入口:卡片双座位**(2026-08 删除左侧导航 settings.section 注册;v0.12.0 起双座位,见下;settings.section 相关纪律——store 传**共享 handle**(apply 创建一次的 viewStore,同 persist 名多个 handle 会交叉污染)、组件用 useStore 读 prefs/actions.setPref 写——对卡片同样适用)。
    - **设置卡片双座位(v0.12.0)**:settings.plugin.item(旧宿主 设置→插件,key 'better-workspace')+ plugins.bundle.config(alpha.2+ 插件面板 bundle 页,**按包名** key 'dsh-better-workspace',渲染时 props.view==='page' → 平铺无折叠壳;旧座位仍折叠卡:标题「更好的工作区」+ desc + Chevron)。两座位都 `{locale:NS, store:viewStore}`,slots.inject 各等各的声明,任一宿主年代恰好一个座位活。tab/面板只派发「宿主已服务命名空间 ∩ 卡片」。
    - **默认外观区(v0.10.0)**:卡片内嵌 `AppearanceControls`(与行级 CustomizeDialog 同一组件,只有 allowIcon 不同),写 `prefs.appearance` 并经 setPref 双写宿主 `appearance` 字段;宿主 schema 用 `Schema.dict(Schema.any())`(形状由 client 半自管,与 styling 同款)。描边默认**开**、宽度默认 1px。
    - **宿主命名空间 schema 必须用 @deepseek-ai/schemastery**:settings 服务把 schema 当**函数调用**(`schema(mergeLayers(...))`);schemastery 的 schema 可调用、传 undefined 补默认值;zod 对象**不可调用**,register() 抛 `TypeError: ... is not a function` 后命名空间从未服务(2026-08 实测根因,zod 版卡片死活不出的原因)。dsh-context / dsh-better-sidebar 同为 schemastery。src/index.js 用动态 `import('@deepseek-ai/schemastery')`(冒烟零依赖);runtime 解析走 profile 共享 fallback(已实测可解析)。
    - **settingsNamespace() 时代(v0.6.0)**:dsh >= 0.1.2-alpha.2 的 @deepseek-ai/dsh-settings **移除了 settingsNamespace() 导出**(installSettingsSection/deepEqualJson 一并移除),register() 改为接受普通字符串。src/index.js 用 `typeof ds.settingsNamespace === 'function'` 探测 —— 新 register() 接受普通字符串,旧 register() 也接受(helper 只是编译期 branding),单次调用双 era 兼容。**改 register() 调用必须先过这个探测**,不要假设 helper 还存在。
14. **透明/磨砂适配纪律**(第三方主题插件(dsh-any-background)实测):自绘表面一律走「主题 token + 插件 CSS 变量」链,禁止写死不透明背景。右键菜单底色 = var(--dsw-specific-menu, var(--dsw-alias-bg-overlay, rgba(28,28,32,.72)))(官方菜单同款 token,插件覆盖它时自动生效);磨砂 = backdrop-filter: var(--dsh-any-blur-card-panels, blur(12px) saturate(1.15))(-webkit- 同步)。其他表面同理:行透明 / 输入用 bg-layer-2 / 弹窗用 primitives Modal(官方面)。新增自绘面板必须复用该链,v0.4.1 已全仓扫过无残留硬背景。
- flat 视图未接管;磁盘层嵌套默认生效,「仅名称」模式下关闭(v0.21.0 三态,issue #7)——该模式下 folders.ws 里按磁盘层 idPrefix 分箱的声明不解析,只有根作用域声明照常渲染。
- 折叠状态与标题缓存每端本地(设计如此);外观/分组/开关 0.9.5 起可经设置卡片手动跨端同步。

15. **新增项自动配色 + 图标(v0.25.0 起双主题 AA 槽位,`prefs.autoStyle`,默认关)**:
    - **落点**:设置卡的 `role="switch"`(与 sessionMenu/rowActions 同款),偏好进视图 store 的 `prefs` 箱(浏览器本地,不进宿主行 Config schema),判据写死 `=== true` ⇒ 任何旧快照缺键=关。
    - **颜色是行文字色**:`rowStyleOf()` 把 `color` 写进 `style.color`(行字号 13px / 会话行 12.5px、常规字重 ⇒ **WCAG normal text**)。v0.25.0 起是**双主题槽位池**:`AUTO_COLOR_SLOTS` 的每个槽位有 `{light, dark}` 两个色,**浅色池的色必须在浅色主题的保守行底上 ≥4.5、深色池的色必须在深色主题的保守行底上 ≥4.5**(正文级 AA;冒烟逐色断言,并对真实底色各再断言一次)。保守行底取自**真机实测**:浅色 `--dsw-alias-bg-base` #ffffff、悬停/当前行叠加约 #f7f8fa ⇒ 判据用 **#ebedf0**;深色 base #151517、layer-2 #2c2c2e ⇒ 判据用 **#2c2c2e**(最亮的面)。**改任何一个色都必须重跑冒烟里的对比度断言**;跨主题的数字只作记录、不作承诺(浅色池的色换到深色主题上会由槽位解析成另一套色)。
    - **存的是槽位 id,不是色值(v0.25.0 的核心设计)**:`styling` 里自动分配的 `color` 是 `'auto:3'` 这样的 **token**,渲染时经 `resolveColorValue(value, dark)` 按当前主题解析 ⇒ **换主题自动换色**,无需迁移、无需重算、跨端同步也天然正确(宿主副本存 token,各端各自上自己的主题色)。**手动颜色仍是 hex 且原样透传、永不被覆盖**;`colorToRgb()` 只认 6 位 hex,所以 token 不会误入描边/取色器逻辑(其中 `strokeColorOf` 的 auto 极色、`AppearanceControls` 的展示值/预览、`rowStyleOf` 都已先 `resolveColorValue`)。**槽位表 append-only**(与 ICON_CHOICES/ICON_ALIASES 同纪律):已存的 token 必须永远解析得出,**越界索引夹到槽位 0**而不是渲染成空色;不要重排/删除槽位。
    - **主题信号 = `document.body[data-ds-dark-theme]`**(官方 `packages/client/ui-theme/src/boot-theme.ts` toggle 的布尔属性),**不要**读 `prefers-color-scheme`(宿主自己解析 `system`,可能与 OS 相反)。`autoThemeDarkNow()` 每次渲染现读属性,`useAutoThemeDark()`(基于 `React.useSyncExternalStore` + body 属性 MutationObserver,React 缺该 API 时退化为直接读取)让相关组件在换主题时重渲染 —— 目前挂在 BetterBrowser 与 AppearanceControls 上。
    - **文案口径(v0.25.0)**:「每个色在**对应主题**背景下 ≥4.5(WCAG 正文级 AA);切换主题自动换成同色系另一套」。冒烟有一条测试**逐门**校验 21 份 hint 都含 `4.5`/`4,5` 与 `AA`,且**不得再出现 `3.0`/`3,0` 旧阈值**——**匹配不要写死语序与小数符号**(ja `コントラスト 4.5 以上(本文の AA)`、ko `대비 4.5 이상(본문 AA)`、de/fr `4,5` 都必须放行)。
    - **抽取 = 洗牌袋 + 近期回避窗口(3)**,不是纯随机(`createAutoStyler`,纯函数、随机源可注入);颜色袋与图标袋独立。
    - **图标池只从既有 `ICON_CHOICES` 里取**,运行时经 `resolveIconName()` 过滤宿主导出;**图标只给工作区**——会话行没有图标位(不变量 12:图标位归官方状态灯 / v0.23.0 起的 `sidebar.session.row.leading` 座位),给会话写 icon 等于写一个永不显示的字段。
    - **「新增」判定不轮询,且两侧都必须先"播种"**:会话 = wire 仍报 `blank === true` **且首份权威会话快照已播种**(`list.phase !== 'pending'`,首帧就 blank 的行只登记不染色);工作区 = `phase === 'ready'` 且流 settle 的**首份权威清单**播种之后出现的 id。**播种标志只在"该快照确实登记了项"时置位**(`if (sessionRowsSeen) …` / `if (workspaceRowsSeen) …`)——冷启动时宿主可能先回一份 `ready` 但**空**的快照,无条件置位会让随后到达的存量行被当成"新增"染色(独立验收抓到 1/8 的偶发窗口);空快照不消耗标志,下一份有内容的快照来播种。⇒ 「只影响开启之后新建的项」在两侧都成立(缺播种会让"打开开关时就已存在的 blank 占位行"在下次加载被染色,这是独立验收抓到的偏差)。已见集合在开关关闭期间同样维护 ⇒ 关闭期间创建的项不会在开启后被补分配。**残余理论窗口(后续项,非缺陷)**:宿主把存量会话列表**分批推送**、且首批里没有 blank 行时,晚到的那个"页面加载前就已存在"的 blank 行仍会被当成新会话染色——当前判据(首份**有内容的**权威快照播种)覆盖"空快照/只含已命名会话/含 blank"三种情形,覆盖不了"分批晚到"。彻底关闭需要宿主给出"列表已收敛"的信号(工作区侧先例 `workspaceStreamState !== 'loading'`;会话侧只有 `phase`,没有等价收敛态),届时把播种条件从"有内容"改成"已收敛"即可,逻辑不必重写。
    - **三条红线**(冒烟各一条断言):`stylingMap[key]` 已存在就跳过(**绝不覆盖**手设/同步带入的样式);同一项幂等(已见集合 + 存在性双门控);关闭只停分配、**不回收**。
    - **批量写**:同一次快照可能分配多个键,`makeSharedWrites.setStylingMany(entries)` 一次写本地 + 宿主副本(逐个 `setStyling` 会让宿主副本只剩最后一个,因为宿主写基于渲染期 `stylingMap` 快照)。

## 验证清单(改动后)

1. npm test 全绿。
2. 真机(web profile 重启 DSH):
   - 两层树渲染:造 web/前端、web/后端(名称分组)、再把某工作区目录嵌到另一工作区目录下(磁盘层),两层正确且正交;
   - 添加工作区(v0.16.0):侧栏按钮在**有后端**时出现 —— loopback = 官方 OS 选择器,选完自动创建工作区并开新会话;0.0.0.0/LAN = 弹出「应用内添加工作区」提示(不再自绘浏览器),添加请在对话空态用官方入口;对话空态的「添加工作区」用的是同一套官方占用者(v0.22.0 起,见下一条);
   - **新建分组(v0.17.0)**:区头图标按钮悬停显示当前可用层(默认「新建会话分组」);点击 → 「新建分组」对话框 = 选择位置(**树状**可展开;会话层直接列工作区,首层默认展开,无工作区时显示空态文案)+ 名称 + 确定。建一个空分组应立即出现在树里;**未选容器点确定应报「请先选择一个工作区」**。空分组右键有「删除分组」,删掉后行消失;非空分组**没有**该项。开启工作区命名分组后对话框应出现**两态单选**并把工作区树作为候选。
   - 右键菜单卫生:官方条目在上,插件条目在分隔线之下;「移动到分组…」有图标(宿主图标集里 `IconFolderOutline16` **不存在**,必须用 `IconFolderOpenOutline16`)。
   - 重命名工作区:跨分组改名(如 web/前端 → design/前端)后树即时重排;磁盘层不变;
   - **工作区分组开关(v0.15.0,默认关闭)**:清掉本页 localStorage 的 dsh.betterWorkspace.view.v1 后刷新 → 树**只按磁盘目录**、工作区标题整条原样(如 `web/前端` 不再拆成组)、区头**无**「新建分组」按钮、工作区行右键**无**「移动到分组…」、设置卡片里该开关为**关**;会话分组照旧(会话行右键仍有「移动到分组…」,会话仍按 / 分组)。设置里打开 → 工作区立刻按 / 分回组、区头按钮与工作区菜单项同时出现。**关掉时拖拽工作区只换顺序,标题绝不被改写**(这是本次改动自身引出、已修的语义空洞)。
   - **开关的升级语义**:老用户模拟——把 localStorage 里 view 快照的 `workspaceTitleSlash` 键**删掉**(其余保留)刷新 → 应**仍按 / 分组**(0.13 以前的快照保持开启);把整个 key 删掉刷新 → 回落到新默认**关闭**;显式存过 false 的仍为关。
   - 会话:打开/重命名/分叉/归档、新会话按钮、当前高亮、运行圆点;
    - **会话拖出分组(v0.18.0)**:把带 `/` 前缀的会话行拖到**它自己的工作区行**上(整行高亮 `bw-drop-into`)→ 松手后分组行消失、会话回到根层末尾;拖到**别组的会话行** → 前缀改写成那个组;拖到根层的会话行 → 移出分组;已在根层的行拖到工作区行**不高亮、不接管**,拖到别人的工作区行同样不接管。带前缀的会话行右键有「移出分组」,根层行没有。**合成拖拽必须分帧派发**——同一 tick 连发 dragstart/dragover/drop 时 React 还没提交 drag 状态,onDragOver 读到的仍是 null(隔离探针踩过这个坑);
   - **重启标题不回退(0.9.4,0.9.6 重写后行为不变)**:让若干会话带 / 标题(含一个 fork 出来的),重启 DSH 后不打开它们,树里应显示 remembered 标题且分组正确(而非工作区名);打开任一会话后标题与官方一致;清掉 localStorage 的 dsh.betterWorkspace.titles.v1 后重启,行为退回官方 basename 回退(兜底不劣化);agent 运行中侧栏应无卡顿(0.9.6 修复 issue #1 的回归点);
   - **对话空态工作区选择器(v0.22.0)**:点空态页的「工作区」按钮 → 弹层是**侧栏那棵树**(分组行带 chevron、工作区行带文件夹图标与缩进、当前工作区带勾选、底部「添加工作区」);侧栏视图选项里改「工作区分组」(磁盘目录 / 磁盘 + 名称 / 仅名称)或「分组方式」→ 弹层**立即**跟着变(同一 store);点工作区行 → 按钮文字与当前工作区切换;点分组行 → 该层开合(选择器内记忆);点「添加工作区」→ 官方占用者(native = OS 选择器 / browse = 官方应用内浏览器)照常弹出。console 不得出现插件自身报错,也不得出现 `register skipped for conversation.hero.workspace`;
   - **rc.2 面(v0.23.0,每次升级后都要过)**:① **⌥⌘K / ⌥⌘O**(web 端官方默认 = primary+alt,桌面端 = 单 primary,即 ⌘K/⌘O)按下后要真的发生事情 —— 搜索键打开并聚焦本插件的过滤框、添加键走 `startAddFlow`(browse 后端 ⇒ add-guide 提示),**不能是「按键被消费但什么都不发生」**;三个按钮的 title/aria-label 要带当前键位。② **视图菜单的归档筛选**是三条直选项(隐藏已归档 / 全部对话(显示已归档)/ 仅显示已归档),选中态恒有一条。③ **仅显示已归档**时没有归档会话的工作区整行消失、空态给「暂无已归档会话」+「查看其他会话」。④ **全新 DSH_HOME 首启**:侧栏显示「默认工作区」而不是裸 `default-workspace`(空态选择器同字)。⑤ 启用官方 Schedule overlay 后,空闲非 blank 会话行上本插件渲染的是**官方座位占用者**(无任务时不绘制属正常;blank 行不给座位)。
   - **自动配色开关(v0.24.0 / v0.25.0 双主题)**:设置卡里打开「新增项自动配色」→ 新建一个会话(工作区行 `+`)与一个新工作区 → 两者都拿到内置色、工作区额外拿到图标;刷新后样式仍在;关闭开关后再新建 → 不再分配,且旧的不回收;对已手动自定义过的行重新加载 → 颜色不被改写。**v0.25.0 追加**:浅色与深色主题**各跑一遍** —— 行内联色必须等于对应池的色值、且现场量到的对比度 ≥4.5;在设置里切浅↔深主题,同一行的颜色要**自动换成同槽位的另一套色**(存储里始终是 `auto:N`,不因换主题改写),换回来颜色也要还原;两种主题下都截图留证。
   - 隔离验证配方(v0.23.0 实测):`PATH=/opt/homebrew/bin:/opt/homebrew/opt/node@24/bin:/usr/bin:/bin SSH_CONNECTION=<非空> DSH_HOME=<隔离目录> dsh web --no-open --port <端口>`;profile 里装本地 tarball + 加进 `dsh.profile.bundles`;playwright 用 dsh 仓库 pnpm store 的 1.61.1,但**本机 chromium 缓存版本(1187)与它期望的 1228 不一致**,要 `chromium.launch({ channel: 'chrome' })` 走本机 Chrome;`NODE_PATH` 对 ESM 无效,需在脚本目录建 `node_modules/playwright` 软链。收尾按端口 kill 残留 node 进程——后台 job 被杀不会带走 dsh 子进程。**首启会有官方弹层(内测声明 → API Key)挡住界面**,自动化要先点「继续」「稍后配置」。
   - **跨端手动同步(0.9.5)**:web 端自定义若干外观 → 设置卡片点「发送本设备数据」→ 桌面端(装同版本插件)设置卡片点「从 Web 获取」(覆盖模式)→ 外观/分组/开关一致;合并模式下两端独有条目并集保留;宿主 ~/.dsh/settings.yaml 的 better-workspace 段可查;宿主 settings 不可用时卡片提示不支持、行为回纯本地;
   - 卸载/禁用本插件 → 回到官方浏览器,hero 流回退官方默认。
3. 升级 dsh 后:对照 packages/client/ui-workspace/src/client/contract/slots.ts 复核 slot 契约与注入面是否漂移(重点:GlobalStandardProps、directoryFlow owner props、single 槽 priority 语义)。
